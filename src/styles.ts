import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#fff',
    },
    secondary: {
      main: '#4fb595',

    },
    warning: {
      main: '#f6ad50',
    },
    error: {
      main: '#ee5463',
    }
  }
});

export const sideDrawerWidth = 270;
